# GitHub GraphQL API token — required for the GitHub Stats section.
# Generate at: https://github.com/settings/tokens
# Required scopes: read:user (public data only, no write access needed)
#
# Without this token the GitHub Stats section is simply hidden;
# the rest of the site builds and runs normally.
GITHUB_TOKEN=ghp_your_token_here
